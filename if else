#include <iostream>
int main()
{
    int age;
    std::cout<<"Enter your age: ";
    std::cin>>age;
    if(age>=18)
    {
        std::cout<<"Your are eligible to vote on election.";
    }
    else
    {
        std::cout<<"Your are not eligible to vote on election.";
    }
}
